Requirements:
 - docker
 - go 


Get mongo docker image:

- `docker pull mongo:latest`
- `docker run mongo`


Run app: 
- make build
- make run

When done:
- make clean

Tests:
- make test

Note:
On `docker` branch is attempt of docker-compose 